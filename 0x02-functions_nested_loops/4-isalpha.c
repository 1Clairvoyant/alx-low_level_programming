#include "main.h"

/**
  * _isalpha - checks for alphabet
  * @c: a character argument to be checked
  *  Return: 0 or 1 depending on the condition
  */

int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (C >= 'A' && C <= 'Z'));
}
